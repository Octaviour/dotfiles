setw -g mode-keys vi

# set prefix to ctrl-space
set-option -g prefix c-@

# vimlike movement between windows
bind h select-pane -L
bind l select-pane -R
bind k select-pane -U
bind j select-pane -D

# split panes
bind - split-window
bind _ split-window
bind | split-window -d
bind \ split-window -d
